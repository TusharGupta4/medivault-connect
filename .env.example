# Base URL for the Django REST backend documented in MEDIVAULT_API_DOCS.md.
# Must end with a trailing slash. Defaults to http://localhost:8000/api/ when unset.
VITE_API_BASE_URL=http://localhost:8000/api/